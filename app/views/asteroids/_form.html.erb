<%= form_with(model: asteroid, local: true) do |form| %>
  <% if asteroid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asteroid.errors.count, "error") %> prohibited this asteroid from being saved:</h2>

      <ul>
      <% asteroid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
